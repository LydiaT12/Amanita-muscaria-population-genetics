# Raw data

The mitogenome variants were called. The SNP quality filtering which was applied to the nuclear genome results in no mitogenome variants, so I stick with just the default GATK filters. I use only SNPs. The dataset is in both bgz and gz compression formats

```
02.Data_quality/mitogenome_SNP.vcf.bgz  02.Data_quality/mitogenome.vcf.gz
```

I need/want a plink dataset to match

```
plink2 --vcf ../02.Data_quality/mitogenome.vcf.gz --allow-extra-chr --make-pgen --out mitogenome
plink2 --pfile mitogenome --allow-extra-chr --max-alleles 2 --make-bed --out mitogenome_no_multiallelic
```

# Filtering? 

If I filter the mitogenome with the same quality settings as the nuclear genome, nothing remains. We do expect different read depths for nuclear vs mitochondrial genomes. 

I try:

```
bcftools filter -i "MQ>=60" mitogenome.vcf.gz -o mitogenome_filtered.vcf.bgz -O z
```
236 sites remain. 

And maybe filtering by MAF? As long as I don't use that dataset for any SFS stuff? Any SNP present in more than one genome is way more trustworthy. I'm a bit sus on there being one single common genome and loads of unique ones. 

```
bcftools filter -i "MAC>1" mitogenome_filtered.vcf.bgz -o mitogenome_filtered_a2.vcf.bgz -O z
```
Leaves 225 sites. 



# Remove clones
```
vcftools --gzvcf mitogenome_filtered_a2.vcf.bgz --remove-indv F11 --remove-indv F2 --remove-indv H3 --remove-indv H4 --remove-indv T10 --remove-indv T18 --remove-indv T4 --recode -c --recode-INFO-all | gzip -c > mitogenome_filtered_a2.noclone.vcf.gz
```

# Phylogeny

I'm going to use the most filtered dataset.

Converting to a phylip file doesn't seem to work when given a bgz file. I need to feed the function a gz file. I tried to copy the bgz files in the 02 folder to gz files in the 06 folder, but I ended up unzipping the files in the 02 folder and making possibly empty files in the 06 folder. So then I re-zipped the files in the 02 folder and worked with them. 

```
bgzip -d ../02.Data_quality/mitogenome_filtered_a2.vcf.bgz | gzip -c > mitogenome_filtered_a2.vcf.gz    #first attempt
cd ../02.Data_quality/
gzip mitogenome_filtered_a2      #gzipping
gunzip -c mitogenome_filtered_a2.vcf.gz | bgzip  > mitogenome_filtered_a2.vcf.bgz    #getting my bgz files back
```

Now I can convert to a phylip file:

```
../05.Population_structure/Phylogeny/vcf2phylip.pl --input mitogenome_filtered_a2.vcf.gz
../05.Population_structure/Phylogeny/vcf2phylip.pl --input mitogenome_filtered.vcf.gz      # I don't want to use this one, but it exists (due to my debugging) 
```

Run IQ-tree (runs inline in a few minutes)
```
iqtree2 -s mitogenome_filtered_a2.min4.phy -st DNA -m GTR+G -bb 1000  -pre inferred
```



<!--
Okay, what have I done to this file?
OGfiles:
02.Data_quality/mitogenome_SNP.vcf.bgz  02.Data_quality/mitogenome.vcf.gz

Are they okay?
../05.Population_structure/Phylogeny/vcf2phylip.pl --input ../02.Data_quality/mitogenome.vcf.gz
Yes this works. 

Then I filtered it: mitogenome_filtered.vcf.bgz

../05.Population_structure/Phylogeny/vcf2phylip.pl --input ../02.Data_quality/mitogenome_filtered.vcf.bgz
Gets the first kind of error. I suspect this is the bgz. 
Try convert zipping
bgzip -d ../02.Data_quality/mitogenome_filtered.vcf.bgz | gzip -c > mitogenome_filtered.vcf.gz
Header error here. 

Okay, I suspect that my bgz->gz conversion is the problem. I used a vcftools function before. Maybe I can adapt that. 
Oh hey, I'd done bcftools merge outgroup_phalloides.vcf.bgz filtered_dataset.vcf.bgz -Oz -o filtered_dataset_phalloides_outgroup.vcf.gz
So what about trying 
bcftools filter -i "MAC>1" mitogenome_filtered.vcf.bgz -o mitogenome_filtered_a2.vcf.gz -Oz
-->




## Outgroup

<!--cp ../02.Data_quality/6.All_datasets/mitogenome_filtered_a2.vcf.bgz mitoa2.vcf.bgz-->

I can subset the outgroups used for the nuclear phylogeny to just the mitogenome

```
bcftools view all_outgroups.vcf.gz -r Scaffold133_mitgenome_circular_Amanita_muscaria  -Oz -o outgroups_mitogenome.vcf.bgz
```

Now I need to merge this with my mitogenome

```
bcftools index outgroups_mitogenome.vcf.bgz
bcftools index mitoa2.vcf.bgz
bcftools merge outgroups_mitogenome.vcf.bgz mitoa2.vcf.bgz -o mito_and_outgroups.vcf.gz
bcftools index mito_and_outgroups.vcf.gz
```

Now I convert to phylip:

```
../05.Population_structure/Phylogeny/vcf2phylip.pl --input mito_and_outgroups.vcf.gz
```

This creates ```mito_and_outgroups.min4.phy```.





### New attempt

Subset the reference genome to just the mitogenome

```
samtools faidx ../01.VariantCalling/1.ref_genome/Amanita_muscaria_genome_2024.fasta Scaffold133_mitgenome_circular_Amanita_muscaria > mitogenome2024.fasta
```

Align the reference and Li mitogenomes and get all the sites at which they differ
```
module load MUMmer
nucmer --maxmatch mitogenome2024.fasta mitogenome/LiNCBImitogenome.fasta
delta-filter -1 -r out.delta > mitogenome_comparison2.filter.delta
show-coords -c mitogenome_comparison2.filter.delta > mitogenome_comparison2.coords

show-snps -Clr mitogenome_comparison2.filter.delta > out2.snps  #
```

Get the locations of population SNPS (wrt the reference) which are in the phylip 
```
bcftools query -f '%CHROM\t%POS\n' mitoa2.vcf.bgz > mito_snps.txt
# bcftools query -f '%CHROM\t%POS\n' ../02.Data_quality/6.All_datasets/mitogenome_SNP.vcf.bgz > mito_snps2.txt
```

Turn this into a regions bed file
```
awk '{print $1 ":" $2}' mito_snps.txt > mito_snp_region.txt
awk '{print $1 ":" $2 "-" $2}' mito_snps.txt > mito_snp_region2.txt
# awk '{print $1 ":" $2 "-" $2}' mito_snps2.txt > mito_snp_bed1.txt
# awk '{print $1 ":" $2}' mito_snps2.txt > mito_snp_region1.txt
```

Get the SNPs of the reference:
```
samtools faidx mitogenome2024.fasta -r mito_snp_region.txt > mitosnps.fasta
# samtools faidx mitogenome2024.fasta -r mito_snp_bed1.txt > mitosnps1.fasta
```

This gives each SNP on a new line. so i convert them to something more like a fasta. I don' think I'm doing this efficiently but oh well. 
```
grep -v '^>' mitosnps.fasta | tr -d '\n' > cat_mitosnps.fasta
#grep -v '^>' mitosnps1.fasta | tr -d '\n' > cat_mitosnps1.fasta
```

Get a list of sites where there is a SNP in the population AND the outgroup differs from the reference
awk 'FNR==NR {
    info[$1] = $2; 
    info2[$1] = $3
    next
} 
{
    lineNum = FNR
    split($1, a, ":")
    if (a[2] in info)
        print lineNum, a[1] ":" a[2], info[a[2]], info2[a[2]]
}' out2.snps mito_snp_region.txt > outgroupsnps.txt

First column should the the position of the variant in the phylip
Second column, the SNP position as a coordinate
Third, fourth columns, the variants involved (ref, outgroup)

awk 'FNR==NR {
    info[$1] = $2; 
    info2[$1] = $3
    next
} 
{
    lineNum = FNR
    split($1, a, ":")
    if (a[2] in info)
        print lineNum, a[1] ":" a[2], info[a[2]], info2[a[2]]
}' out2.snps mito_snp_region1.txt > outgroupsnps1.txt


Finally, I want to go back to the fasta fo the SNPs in the reference, and change the ones in outgroupsnps.txt. I did that manually. Now I have cat_mitosnps_changed.fasta , which contains the population SNPs with their outgroup values. 


Convert the main mito file to a phylip:

../05.Population_structure/Phylogeny/vcf2phylip.pl --input ../02.Data_quality/mitogenome.vcf.gz

I want a fasta file with the reference genome values at each SNP site


