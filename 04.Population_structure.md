<!--
# K means

## Initial analysis
 module load fastStructure/1.0-gimkl-2020a-Python-2.7.18
Basic bit of code is:
```
structure.py -K 1 --input ../../02.Data_quality/4.PLINK/filtered_dataset_no_multiallelic --output filtered_dataset
```

I'm going to want to do this in a loop, trying different values of K

```
for k_val in {2..8}; do
structure.py -K ${k_val} --input ../../02.Data_quality/4.PLINK/filtered_dataset_no_multiallelic --output filtered_dataset;
done 
```


Having tested for a bunch of values of K, we test which one is optimal:

```
chooseK.py --input=filtered_dataset
````
Model complexity that maximizes marginal likelihood = 1
Model components used to explain structure in data = 2

## Second parse
Following advice, I re-run with a logistic prior and try both K=1 and K=2

```
structure.py -K 1 --prior logistic --input ../../02.Data_quality/4.PLINK/filtered_dataset_no_multiallelic --output filtered_dataset_logistic
structure.py -K 2 --prior logistic --input ../../02.Data_quality/4.PLINK/filtered_dataset_no_multiallelic --output filtered_dataset_logistic
```

I try to plot the admixtures:
```
distruct.py -K 1 --input filtered_dataset_logistic --output filtered_dataset_logisticK1.svg
```
But this gives an error because it can't load matplotlib. 
-->



# PCA


# F_ST

```
vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/F_inds.txt --weir-fst-pop ../99.vcftools/H_inds.txt --weir-fst-pop ../99.vcftools/S_inds.txt --weir-fst-pop ../99.vcftools/T_inds.txt --weir-fst-pop ../99.vcftools/W_inds.txt --out FST
```
> Weir and Cockerham mean Fst estimate: 0.011343
> Weir and Cockerham weighted Fst estimate: 0.019237

Is this significant? I don't know. I can look at samples pairwise. 

```
vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/F_inds.txt --weir-fst-pop ../99.vcftools/W_inds.txt --out FST_FW
vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/H_inds.txt --weir-fst-pop ../99.vcftools/W_inds.txt --out FST_HW
vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/S_inds.txt --weir-fst-pop ../99.vcftools/W_inds.txt --out FST_SW
vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/T_inds.txt --weir-fst-pop ../99.vcftools/W_inds.txt --out FST_TW

vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/F_inds.txt --weir-fst-pop ../99.vcftools/T_inds.txt --out FST_FT
vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/H_inds.txt --weir-fst-pop ../99.vcftools/T_inds.txt --out FST_HT
vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/S_inds.txt --weir-fst-pop ../99.vcftools/T_inds.txt --out FST_ST

vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/F_inds.txt --weir-fst-pop ../99.vcftools/S_inds.txt --out FST_FS
vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/H_inds.txt --weir-fst-pop ../99.vcftools/S_inds.txt --out FST_Hs
vcftools --gzvcf ../02.Data_quality/filtered_dataset_thinned_noclone.vcf.gz --weir-fst-pop ../99.vcftools/F_inds.txt --weir-fst-pop ../99.vcftools/H_inds.txt --out FST_FH
```

> F-W:
> Weir and Cockerham mean Fst estimate: 0.0068458
> Weir and Cockerham weighted Fst estimate: 0.010801
> H-W
> Weir and Cockerham mean Fst estimate: 0.010884
> Weir and Cockerham weighted Fst estimate: 0.019879
> S-W:
> Weir and Cockerham mean Fst estimate: 0.0048191
>Weir and Cockerham weighted Fst estimate: 0.0083298
> T-W:
> Weir and Cockerham mean Fst estimate: 0.007062
> Weir and Cockerham weighted Fst estimate: 0.015766
> F-T:
> Weir and Cockerham mean Fst estimate: 0.020318
> Weir and Cockerham weighted Fst estimate: 0.035182
> H-T:
> Weir and Cockerham mean Fst estimate: 0.026443
> Weir and Cockerham weighted Fst estimate: 0.049927
> S-T:
> Weir and Cockerham mean Fst estimate: 0.024667
> Weir and Cockerham weighted Fst estimate: 0.04445
> F-S:
> Weir and Cockerham mean Fst estimate: 0.0079191
> Weir and Cockerham weighted Fst estimate: 0.011116
> H-S:
> Weir and Cockerham mean Fst estimate: 0.0081776
> Weir and Cockerham weighted Fst estimate: 0.012361
> F-H:
> Weir and Cockerham mean Fst estimate: 0.0051165
> Weir and Cockerham weighted Fst estimate: 0.008064

The differentiations between W and T or W and H are much higher than those between W and F or W and S. 


# Shared unique variants

I want to find the variants which occur in exactly two populations. For every pair of populations, I want to find the variants which exist in both. Then I want to compare this dataset to the other three populations and remove any variants which are found in one of the other three. 

Before using bcftools I need to bgzip the files. i want to keep the normal gzipped forms, so I'll first copy each population-specific dataset, then bgzip them

```
cp filtered_dataset_F.vcf.gz F.vcf.gz
gunzip -c F.vcf.gz | bgzip  > F.vcf.bgz
```

<!--
Print a list of records which are present in A and B but not in C and D: #the n command reports which files to do
    bcftools isec -n~1100 -c all A.vcf.gz B.vcf.gz C.vcf.gz D.vcf.gz
    -->

```
bcftools isec -n~11000 -p privatevariants/FandH F.vcf.bgz H.vcf.bgz S.vcf.bgz T.vcf.bgz W.vcf.bgz
```


These isec files are all empty. I find it hard to believe that out of 300000+ variants there are _none_ which occur in exactly two populations. Let's try just finding the intersection of two:

```
bcftools isec -n=2 -p privatevariants/FandH/try2 F.vcf.bgz H.vcf.bgz
```

```
bcftools isec -n~01100 -p privatevariants/HandS F.vcf.bgz H.vcf.bgz S.vcf.bgz T.vcf.bgz W.vcf.bgz
head privatevariants/HandS/sites.txt 
# empty

bcftools isec -n~00010 -p privatevariants/Tonly F.vcf.bgz H.vcf.bgz S.vcf.bgz T.vcf.bgz W.vcf.bgz
ls privatevariants/Tonly/
0000.vcf  0001.vcf  0002.vcf  0003.vcf  0004.vcf  README.txt  sites.txt

bcftools isec -n=2 -p privatevariants/FandH/try2 F.vcf.bgz H.vcf.bgz
ls privatevariants/FandH/try2/
0000.vcf  0001.vcf  README.txt  sites.txt

bcftools isec -p privatevariants/FandH/try3 F.vcf.bgz H.vcf.bgz
ls privatevariants/FandH/try3/
0000.vcf  0001.vcf  0002.vcf  0003.vcf  README.txt  sites.txt

bcftools isec -n=2 -p privatevariants/duos F.vcf.bgz H.vcf.bgz S.vcf.bgz T.vcf.bgz W.vcf.bgz
ls privatevariants/duos/
0000.vcf  0001.vcf  0002.vcf  0003.vcf  0004.vcf  README.txt  sites.txt

bcftools isec -n =4 -c all -p privatevariants/quads F.vcf.bgz H.vcf.bgz S.vcf.bgz T.vcf.bgz W.vcf.bgz
ls privatevariants/quads/
0000.vcf  0001.vcf  0002.vcf  0003.vcf  0004.vcf  README.txt  sites.txt
```
So what did I learn? using -n=x will strip out all variants which do not occur in exactly x files. I get 0 variants; empty files. I should have tried x=1 because I find this hard to believe.
Not having an -n command, I get private a file of private alleles for each, and two files which each subset one file to the variants present in the other. 
The ~ binary string of -n should in theory get alleles found in the 1 files and not in the 0 files, but I'm getting nothing... which also happens with -n=x






### Removing invariant sites

<!--
bcftools view -c 1 ../F.vcf.bgz -Oz -o Fvars.vcf.bgz
-->

This gives me a file with the 6857 variants which are unique to site F:

```
bcftools view -x --samples-file 7.SiteSpecific/F_inds.txt 6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o 7.SiteSpecific/Funique.vcf.bgz --force-samples
```

-x says to get the unique variants. --samples-file directs to a list of samples that I'm interested in. --force-samples makes it skip the clones, which are listed in the samples file but aren't in the dataset. 


I made files FandH.txt with the samples from each pair of forests. 

```
bcftools view -x --samples-file FandH.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o FandH.vcf.bgz --force-samples
bcftools view -x --samples-file FandS.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o FandS.vcf.bgz --force-samples
bcftools view -x --samples-file FandT.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o FandT.vcf.bgz --force-samples
bcftools view -x --samples-file FandW.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o FandW.vcf.bgz --force-samples

bcftools view -x --samples-file HandS.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o HandS.vcf.bgz --force-samples
bcftools view -x --samples-file HandT.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o HandT.vcf.bgz --force-samples
bcftools view -x --samples-file HandW.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o HandW.vcf.bgz --force-samples
bcftools view -x --samples-file SandT.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o SandT.vcf.bgz --force-samples
bcftools view -x --samples-file SandW.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o SandW.vcf.bgz --force-samples

bcftools view -x --samples-file TandW.txt ../../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o TandW.vcf.bgz --force-samples
```

Index:
```
for forest in F H S T; do bcftools index ${forest}andW.vcf.bgz; done
for forest in F H S; do bcftools index ${forest}andT.vcf.bgz; done
for forest in F H; do bcftools index ${forest}andS.vcf.bgz; done
bcftools index FandH.vcf.bgz
```

Then I make files of unique variants
```
for forest in F H S T W; do bcftools view -x --samples-file ${forest}_inds.txt ../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o ${forest}_only.vcf.bgz --force-samples; done

for forest in F H S T W; do bcftools index ${forest}_only.vcf.bgz; done
```

Now, for each pair I want to remove the singletons:

```
bcftools isec -n~100 -p FandH FandH.vcf.bgz F_only.vcf.bgz H_only.vcf.bgz
```

File 0000.vcf contains the 2694 variants which were in (ForH) but are not unique to F or to H

```
bcftools isec -n~100 -p FandS FandS.vcf.bgz F_only.vcf.bgz S_only.vcf.bgz
bcftools isec -n~100 -p FandT FandT.vcf.bgz F_only.vcf.bgz T_only.vcf.bgz
bcftools isec -n~100 -p FandW FandW.vcf.bgz F_only.vcf.bgz W_only.vcf.bgz

bcftools isec -n~100 -p HandS HandS.vcf.bgz H_only.vcf.bgz S_only.vcf.bgz
bcftools isec -n~100 -p HandT HandT.vcf.bgz H_only.vcf.bgz T_only.vcf.bgz
bcftools isec -n~100 -p HandW HandW.vcf.bgz H_only.vcf.bgz W_only.vcf.bgz

bcftools isec -n~100 -p SandT SandT.vcf.bgz S_only.vcf.bgz T_only.vcf.bgz
bcftools isec -n~100 -p SandW SandW.vcf.bgz S_only.vcf.bgz W_only.vcf.bgz
bcftools isec -n~100 -p TandW TandW.vcf.bgz T_only.vcf.bgz W_only.vcf.bgz
```


### rand_sim folder

The file all_noclone_inds.txt contains the codes for all individuals, with only one sopy of each clone. ** Note: it doesn't look like the clone representatives I kept in here match the ones in the vcf. I'll have to re-do this **
Make files with random sets of 14 sampled individuals

```
nn=14
for ii in {1..99}; do shuf -n ${nn} ../../all_noclone_inds.txt > ${nn}sample${ii}.txt; done
```

Now I get the variants unique to each sample and make a file with the stats. i can read the :
```
for ii in {1..99}; do bcftools view -x --samples-file ${nn}sample${ii}.txt ../../../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o ${nn}sample${ii}.vcf.bgz --force-samples; done

for ii in {1..99}; do bcftools index ${nn}sample${ii}.vcf.bgz; done

for ii in {1..99}; do bcftools view -H ${nn}sample${ii}.vcf.bgz | wc -l; done
```



#### Now I need to do pairwise comparisons

```
nn=14
mm=15

echo "nn=${nn}" >> numsharedvariants_${nn}_${mm}
echo "mm=${mm}" >> numsharedvariants_${nn}_${mm}

for jj in {1..99};
do
k1=$((1 + $RANDOM % 99)) 
k2=$((1 + $RANDOM % 99))

# test: can I give bcftools two samplesfile inputs?
bcftools view -x --samples-file ${nn}sample${k1}.txt --samples-file ${mm}sample${k2}.txt ../../../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o ${nn}${mm}_${k1}and${k2}.vcf.bgz

# if not, I might need to make a file
cp ${nn}sample${k1}.txt ${nn}${mm}_${k1}and${k2}.txt
${mm}sample${k2}.txt >> ${nn}${mm}_${k1}and${k2}.txt
bcftools view -x --samples-file ${nn}${mm}_${k1}and${k2}.txt ../../../6.All_datasets/filtered_dataset_noclone.vcf.gz -Oz -o ${nn}${mm}_${k1}and${k2}.vcf.bgz

bcftools isec -n~100 -p ${nn}${mm}_${k1}and${k2}${nn}${mm}_${k1}and${k2}.vcf.bgz ${nn}sample${k1}.vcf.bgz ${mm}sample${k2}.vcf.bgz

bcftools view -H ${nn}sample${ii}.vcf.bgz | wc -l >> numsharedvariants_${nn}_${mm}
;
done
```




