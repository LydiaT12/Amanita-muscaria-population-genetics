
# K means
<!-- module load fastStructure/1.0-gimkl-2020a-Python-2.7.18 -->
Basic bit of code is:
```
structure.py -K 1 --input ../../02.Data_quality/4.PLINK/filtered_dataset_no_multiallelic --output filtered_dataset
```

I'm going to want to do this in a loop, trying different values of K

```
for k_val in {2..8}; do
structure.py -K ${k_val} --input ../../02.Data_quality/4.PLINK/filtered_dataset_no_multiallelic --output filtered_dataset;
done 
```


<!--
Having tested for a bunch of values of K, we test which one is optimal:

```
chooseK.py --input=scaffolds1to28
````
Model complexity that maximizes marginal likelihood = 1
Model components used to explain structure in data = 1

Sooo... no grouping. 
-->


# Phylogeny

The A. phalloides genome only has about 1 in 100 sites not an N. I'm not convinced that's going to be enough data to usefully root the tree. Idk. 
Also, my genomes have some entries which are *. Apparently these represent deletions, so I should be okay once I re-do the dataset with the SNP0nly dataset (hopefully!)

<!--
Copy datasets in
```
cp ../../01.VariantCalling/8.outgroups/attempt2/outgroup_phalloides.vcf.gz outgroup_phalloides.vcf.bgz
cp ../../01.VariantCalling/8.outgroups/attempt2/outgroup_phalloides.vcf.gz.csi outgroup_phalloides.vcf.bgz.csi
cp ../../02.Data_quality/filtered_dataset.vcf.bgz filtered_dataset.vcf.bgz
bcftools index filtered_dataset.vcf.bgz 
```
-->


Merge the muscaria dataset with the phalloides outgroup
```
bcftools merge outgroup_phalloides.vcf.bgz filtered_dataset.vcf.bgz -Oz -o filtered_dataset_phalloides_outgroup.vcf.gz
```
Warnings/errors:
Trying to merge "MQ" tag definitons of different types   # I don't think I'm going to use them

