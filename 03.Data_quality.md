# Filtering

I want to filter out anything that looks like it might be an error. To do this, I need to identify outliers. The first things to check are read depth and mapping quality. These give us an idea of how much we trust the output from the haplotype calling, and may suggest areas in the reference genome assembly which are messy. 

## Indels

Indels are more likely than SNPs to be errors [where did I read that?]. They comprise a small proportion of the variants. I play it safe, and filter them all out. 

```
vcftools --gzvcf 1.subset_files/test_scaffold1.vcf.gz --max-alleles 2 --recode -c --recode-INFO-all | gzip -c > 1.subset_files/scaffold1_biallelic.vcf.gz
vcftools --gzvcf 1.subset_files/test_scaffold1.vcf.gz --remove-indels --recode -c --recode-INFO-all | gzip -c > 1.subset_files/scaffold1_noindels.vcf.gz
vcftools --gzvcf 1.subset_files/scaffold1_biallelic.vcf.gz --remove-indels --recode -c --recode-INFO-all | gzip -c > 1.subset_files/scaffold1_biallelic_noindels.vcf.gz
```

The -recode-INFO-all tag is needed to keep important metadata when changing VCF files in vcftools.

## SNP quality

The likelihood that the variant calling algorithm puts that the called SNP is correct is measured by a mapping quality score. I want to filter out all variants with a low mapping quality. 

Read depth measures the number of reads which aligned to a site. It should be fairly even across the genome. Regions where the read depth differs from the baseline value are more likely to have erroneous SNPs - due to misassembly of the reference genome or too few reads to correctly infer heterozygosity when it is present, or excess reads inaccurately mapped to the site and introducing spurious "variation". I need to find out what the baseline read depth is for these mappings and filter out everything too far from the baseline. 

The first step is to find out what the distribution of read depths and mapping qualities is, so I can choose thresholds for filtering. The R function chromoR lets me plot these features for individual scaffolds. Below are the plots for scaffolds 2 and 3, which represent different extremes of quality. It looks like the baseline read depth is less than 5000, and the asscosiated the mapping quality greater than 50. Phred-scaled qualities should work in a bigger-is-better sense, and yet the only outliers I see are high quality scores, so I assume they're all good. 

Scaffold 2: ![image](https://github.com/LydiaT12/Amanita-muscaria-population-genetics/assets/124113370/7c729c15-324a-4c40-820a-6fa113dd3c14)

Scaffold 3: ![image](https://github.com/LydiaT12/Amanita-muscaria-population-genetics/assets/124113370/3dde062b-721e-4fa2-a87c-2b155f0886c2)

<!--
To do:
Filter at those named levels.
Plot the chromoR objects for the filtered dataset.
Obtain new filtering levels.
Filter at new levels.
Plot the chromoR objects for the filtered dataset.
Repeat if appropriate.
Filter at some stupidly strict level.

Obtain 4(+) datasets; unfiltered, filtered I, filtered II, strict
Test relatedness stats for each of the four datasets. 
-->

Read depth in chromR is a sum over all samples. 
vcftools filters by mean depth
"Just to clarify the vcftools behavior in this case, the --minDP filter is a genotype filter, so it looks for the DP tag in the FORMAT field and not in the INFO field."
I'll want to normalise for vcftools because that filters by mean depth. 5000 sum ~ 500 mean. uhh... the vcftools filtering is being weird
Try BCFtools?

AVG(GQ)>50 average (arithmetic mean) of genotype qualities bigger than 50
MIN(DP)>10 & MIN(DV)>3

DP refers to the overall read depth from all target samples supporting the genotype call. (>10)
MQ refers to the root mean square (RMS) mapping quality of all the reads spanning the given variant site. (MQ >= 60)
QD is variant confidence adjusted for variant sites with deep coverages. QD is a better metric than DP or QUAL for variant filtering. Variants with QD < 2 should be removed.

Sensible filters might be:
MQ >= 60
DP>10
GQ>50

```
bcftools filter -i 'INFO/DP>200' test2999.vcf.bgz -o bcffilter1to28.vcf.bgz -O z
bcftools filter -i 'INFO/DP<600' test2999.vcf.bgz -o bcffilter2999.dpu600.vcf.bgz -O z #this is unreasonable. I think bcftools is filtering by total read depth, so will re-try with 5000
bcftools filter -i 'INFO/DP<5000' test2999.vcf.bgz -o bcffilter2999.dpu5000.vcf.bgz -O z #I probably want between 1000 and 3000
bcftools filter -i 'INFO/DP<3000 & INFO/DP>1000' test2999.vcf.bgz -o bcffilter2999.dpu3000o1000.vcf.bgz -O z #yea dat getting good
bcftools filter -i 'MQ>=60' test2999.vcf.bgz -o bcffilter2999.mq60.vcf.bgz -O z #good filter
bcftools filter -i 'MQ>=55 & INFO/DP<5000' test2999.vcf.bgz -o bcffilter2999.mq55Dp5000.vcf.bgz -O z 
```

there's something wrong with the file scaffolds_1to28_filtered.vcf.gz

Examples:
```
vcftools --gzvcf scaffolds_1to28_filtered.vcf.gz --max-meanDP 5000 --minQ 50 --recode -c --recode-INFO-all | gzip -c > 1.subset_files/scaffolds_1to28.read5000Q50.vcf.gz
vcftools --gzvcf scaffolds_1to28_filtered.vcf.gz --max-meanDP 4000 --min-meanDP 500 --minQ 55 --recode -c --recode-INFO-all | gzip -c > 1.subset_files/scaffolds_1to28.read4000Q55.vcf.gz
```



## Thinning

Thin the variants so that the vcf is small enough to work with in R, while still covering all 28 super-contigs. 

```
vcftools --gzvcf scaffolds_1to28_filtered.vcf.gz --thin 500  --recode -c --recode-INFO-all | gzip -c > 1.subset_files/thinned_1to28.vcf.gz
# Ensure that no two variants are less than 500 bases apart
```




# Convert to single-scaffold datasets

For some analyses I will want datasets which comprise a single scaffold. 

```
vcftools --gzvcf scaffolds_1to28_filtered.vcf.gz --chr Scaffold1_Amanita_muscaria  --recode -c --recode-INFO-all | gzip -c > 1.subset_files/test_scaffold1.vcf.gz
```
The -recode-INFO-all tag is needed to keep important metadata

```
for ii in {1..28}; do vcftools --gzvcf scaffolds_1to28_filtered.vcf.gz --chr Scaffold${ii}_Amanita_muscaria  --recode -c --recode-INFO-all | gzip -c > 1.subset_files/test_scaffold${ii}.vcf.gz ; done
```









