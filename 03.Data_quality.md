# Statistics

cp ../01.VariantCalling/7.filtered_variant_files/fullgenome_allvariants.vcf.gz fullgenome_allvariants.vcf.gz
cp ../01.VariantCalling/7.filtered_variant_files/fullgenome_SNP.vcf.gz fullgenome_SNP.vcf.gz
cp ../01.VariantCalling/7.filtered_variant_files/fullgenome_indel.vcf.gz fullgenome_indel.vcf.gz

For bcftools I first have to bgzip the files
```
gunzip -c fullgenome_allvariants.vcf.gz | bgzip  > fullgenome_allvariants.vcf.bgz #error: unexpected end of file. 
gunzip -c fullgenome_SNP.vcf.gz | bgzip  > fullgenome_SNP.vcf.bgz
gunzip -c fullgenome_indel.vcf.gz | bgzip  > fullgenome_indel.vcf.bgz
```

It seems like something went wrong when trying to merge the SNP and indel datasets. I ignore that for now. 

Having converted the SNP and indel datasets to bgz compression, I can calculate stats
```
bcftools stats fullgenome_SNP.vcf.bgz > stats/fullgenomeSNP.stat
bcftools stats fullgenome_indel.vcf.bgz > stats/fullgenomeindel.stat
```

The SNP dataset contains 1,270,039 SNPs. Of these, 73,314 are multiallelic sites and 38,167 are multiallelic SNP sites (no, I'm not sure what the difference is)

The indel dataset contains 82,869 indels. Of these, 8793 are multiallelic. 


# Filtering

The dataset has already had one layer of filtering, using the default values as advised by GATK best practices. However these were just default thresholds. Erroneous variants might remain in the datasets. I want to filter out anything which might be an error. In good news, this dataset contains more than 1.3 million variants, so I can afford to take a fairly brutal filtering approach and still expect to have plenty of data. 

## Indels

Indels are more likely than SNPs to be errors [where did I read that?]. They comprise a small proportion of the variants. I play it safe, and work with only the SNP dataset. (Also, that's convenient because I can continue to ignore the problem of how to properly merge the SNP and indel datasets)

## SNP quality

Each SNP has several statistics which measure aspects of the likelihood that it was called correctly. The two things I focus on are mapping quality and read depth. 

The likelihood that the variant calling algorithm puts that the called SNP is correct is measured by a mapping quality score. I want to filter out all variants with a low mapping quality. 

Read depth measures the number of reads which aligned to a site. It should be fairly even across the genome. Regions where the read depth differs from the baseline value are more likely to have erroneous SNPs - due to misassembly of the reference genome or too few reads to correctly infer heterozygosity when it is present, or excess reads inaccurately mapped to the site and introducing spurious "variation". 

To do effective filtering, I need to figure out what read depth and mapping quality I should generally expect from this dataset. Most SNPs should be fairly reliable. SNPs with outlier values for these two statistics are the sus ones which I'll want to filter out. 

<!-- try this -->
### Convert to single-scaffold datasets

For the next step I want a dataset which comprise a single scaffold. I've done this with vcftools in the past, but I think I can do it now with bcftools.

```
bcftools index fullgenome_SNP.vcf.bgz
bcftools view fullgenome_SNP.vcf.bgz -r Scaffold1_Amanita_muscaria  -Oz -o scaffold1_SNP.vcf.bgz
```

### Quality mapping

The vcfR package in R can visualise quality measures along a genome. It can only plot one scaffold at a time, and the full SNP dataset is too big to read (I could increase the memory, but why bother), so I just look at Scaffold 1 (for now). 

In R:
```
library(poppr)
library(vcfR)
vcf_file_filt1 <- read.vcfR("~/nobackup/whole_genome_resequencing/02.Data_quality/scaffold1_SNP.vcf.bgz")

vars1 <- create.chromR(vcf_file_filt1, name="1")
pcrhom1 <- proc.chromR(vars1)
chromoqc(pcrhom1)
```

This produces the plot below:

![image](https://github.com/LydiaT12/Amanita-muscaria-population-genetics/assets/124113370/d57677fb-683a-4acc-89a1-c847af3ae321)


We see that read depth and mapping quality both vary along the contig. Quality score is variable and hard to interpret (that's a known thing) so we ignore it. It looks like the baseline read depth is less than 5000, and the asscosiated the mapping quality greater than 50. Phred-scaled qualities should work in a bigger-is-better sense, and yet the only outliers I see are high quality scores, so I assume they're all good. 


### Quality filtering
<!--
DP refers to the overall read depth from all target samples supporting the genotype call. Same value as chromR reports
MQ refers to the root mean square (RMS) mapping quality of all the reads spanning the given variant site. (MQ >= 60)
QC is the genotype quality

Sensible filters might be:

```
bcftools filter -i 'INFO/DP<5000' test2999.vcf.bgz -o bcffilter2999.dpu5000.vcf.bgz -O z #I probably want between 1000 and 3000
bcftools filter -i 'INFO/DP<3000 & INFO/DP>1000' test2999.vcf.bgz -o bcffilter2999.dpu3000o1000.vcf.bgz -O z #yea dat getting good
bcftools filter -i 'MQ>=60' test2999.vcf.bgz -o bcffilter2999.mq60.vcf.bgz -O z #good filter
bcftools filter -i 'MQ>=55 & INFO/DP<5000' test2999.vcf.bgz -o bcffilter2999.mq55Dp5000.vcf.bgz -O z 
```
-->
I'm still not entirely sure how strictly I should filter, so I try out a range of stringencies and see what happens. 

A sensible array of filters might be:

```
# OG dataset
fullgenome_SNP.vcf.bgz #this is too big to read into R; R crashes. 
# Lenient filtering
bcftools filter -i 'INFO/DP<3000 & INFO/DP>1000 & MQ>50' fullgenome_SNP.vcf.bgz -o filtration_levels/bcffilter1.vcf.bgz -O z #results in 562,799 variants
# Full filtering
bcftools filter -i 'INFO/DP<3000 & INFO/DP>1000 & MQ>=60' fullgenome_SNP.vcf.bgz -o filtration_levels/bcffilter2.vcf.bgz -O z #350,586 variants
# Extreme filtering
bcftools filter -i 'INFO/DP<2500 & INFO/DP>1500 & MQ>=60' fullgenome_SNP.vcf.bgz -o filtration_levels/bcffilter3.vcf.bgz -O z #313,503 variants     #originally had '& GC>50', but that got an error saying GC isn't defined in the header
```

chromR plot from the bcffilter1 file:

![image](https://github.com/LydiaT12/Amanita-muscaria-population-genetics/assets/124113370/f1db41a5-9a52-4acd-937f-a7411d19cff3)

<!--
Examples:
```
vcftools --gzvcf scaffolds_1to28_filtered.vcf.gz --max-meanDP 5000 --minQ 50 --recode -c --recode-INFO-all | gzip -c > 1.subset_files/scaffolds_1to28.read5000Q50.vcf.gz
vcftools --gzvcf scaffolds_1to28_filtered.vcf.gz --max-meanDP 4000 --min-meanDP 500 --minQ 55 --recode -c --recode-INFO-all | gzip -c > 1.subset_files/scaffolds_1to28.read4000Q55.vcf.gz
```
-->

Gotta index these things for some reason
```
bcftools index filtration_levels/bcffilter1.vcf.bgz
bcftools index filtration_levels/bcffilter2.vcf.bgz
bcftools index filtration_levels/bcffilter3.vcf.bgz
```


## Contig quality

The genome is comprised of 241 supercontigs. The largest 28 of these contain half of the bases in the genome. The other 213 are, logically, much smaller. Some of them are quite small indeed. The small supercontigs are small because they just haven't been matched up with thier neighbour sequences. However, for some of them the reason why they haven't been able to assemble into a larger super-contig might be that the small supercontig is missassembled and doesn't represent anything in the genome, or because it is a duplicate of a sequence elsewhere in the genome. Thus, I hypothesise that the smaller supercontigs are more likely to have dodgy assemblies. This might affect SNP quality. I don't know. But I'm going to look at the effect of subsetting the dataset to exclude small supercontigs. The smallest thing I'm going to allow in this reduced dataset is the mitogenome. 


Make a file with the length of each contig

```
module load bioawk
bioawk -c fastx '{print $name length($seq)}' ../01.VariantCalling/1.ref_genome/Amanita_muscaria_genome_2024.fasta > scaffold_coordinates.list
```
Then I went and maunally edited the file to the format 
> Scaffoldname  1  length

Now I can copy to scaffold_coordinates.1to133.list and delete all the lines after Scaffold 133, and use this as the regions file to subset the vcf

```
bcftools view 2.filtration_levels/fullgenome_SNP.vcf.bgz -R scaffold_coordinates.1to133.list  -Oz -o 2.filtration_levels/scaffolds1to133_SNPfiltered.vcf.bgz
bcftools view 2.filtration_levels/bcffilter3.vcf.bgz -R scaffold_coordinates.1to133.list  -Oz -o 2.filtration_levels/scaffolds1to133_SNPfiltered.vcf.bgz
```

## Filter out multiallelic sites

# PLINK pgen and bed files

Several downstream analyses cannot handle VCF files and instead expect .bed or the newer .pgen file types, which are produced by PLINK. The latest version, PLINK2, uses .pgen files which contain more information, but many applications need the older .bed format. I create both. 

PLINK has an automatic assumption that variants exist in fully assembled chromosomes numbered 1,2,... Instead, I have supercontigs named Scaffold1_Amanita_muscaria, etc. The tag --allow-extra-chr causes PLINK to accept which aren't numbered.  


```
plink2 --vcf ../1.input_datasets/fullgenome_SNP.vcf.bgz --allow-extra-chr --make-pgen --out filter0
plink2 --vcf ../2.filtration_levels/bcffilter1.vcf.bgz --allow-extra-chr --make-pgen --out filter1
plink2 --vcf ../2.filtration_levels/bcffilter2.vcf.bgz --allow-extra-chr --make-pgen --out filter2
plink2 --vcf ../2.filtration_levels/bcffilter3.vcf.bgz --allow-extra-chr --make-pgen --out filter3
```

<!--
```
plink2 --vcf ../2.filtration_levels/bcffilter1.vcf.bgz --allow-extra-chr --make-pgen --out filter1
plink2 --pfile filter1 --allow-extra-chr --make-bed --out filter1
```

Maybe I don't want to do that. [I might need to rename my scaffolds with just numbers. ] The tag --chr-set changes the chromosome set (I think the default assumption is human). The next number is the number of autosomes (limited to 95), and I can add no-xy no-mt to tell it that there isn't a sex chromosome or a mitogenome in the dataset. If I'm going to do this, I might just exclude Scaffolds 1 and 36 (where the MAT loci are) and 133 (the mitogenome)

```
plink2 --vcf test_2to6.vcf.gz --chr-set 28 no-xy no-mt --make-pgen --sort-vars --out testp2
plink2 --pfile testp2 --chr-set 28 no-xy no-mt --make-bed --out testpb2
```
-->


<!--
```
vcftools --gzvcf test_1to28_bi.vcf.gz --chr 2 --chr 3 --chr 4 --chr 5 --chr 6  --recode -c --recode-INFO-all | gzip -c > test_2to6.vcf.gz

plink2 --vcf test_2to6.vcf.gz --chr-set 28 no-xy no-mt --make-pgen --sort-vars --out testp2
plink2 --pfile testp2 --chr-set 28 no-xy no-mt --make-bed --out testpb2
```
-->


# Thinning
<!-- Assuming that the dataset is still quite large -->

Thin the variants so that the vcf is small enough to work with in R, while still covering all 28 super-contigs. 

```
vcftools --gzvcf 2.filtration_levels/bcffilter1.vcf.bgz --thin 500  --recode -c --recode-INFO-all | gzip -c > 5.thinned/filter1.thin.vcf.gz
vcftools --gzvcf 2.filtration_levels/bcffilter2.vcf.bgz --thin 500  --recode -c --recode-INFO-all | gzip -c > 5.thinned/filter2.thin.vcf.gz
vcftools --gzvcf 2.filtration_levels/bcffilter3.vcf.bgz --thin 500  --recode -c --recode-INFO-all | gzip -c > 5.thinned/filter3.thin.vcf.gz
vcftools --gzvcf 2.filtration_levels/scaffolds1to133_SNPfiltered.vcf.bgz --thin 500  --recode -c --recode-INFO-all | gzip -c > 5.thinned/filter4.thin.vcf.gz
# Ensure that no two variants are less than 500 bases apart
```


# Testing the effect of filtration sets

### King relatedness

```
plink2 --pfile filter1 --allow-extra-chr --make-king --out filter1
plink2 --pfile filter1 --allow-extra-chr --make-king-table --king-table-filter 0.25 --out filter1
plink2 --pfile filter0 --allow-extra-chr --make-king-table --king-table-filter 0.2 --out filter0
plink2 --pfile filter2 --allow-extra-chr --make-king-table --king-table-filter 0.2 --out filter2
plink2 --pfile filter3 --allow-extra-chr --make-king-table --king-table-filter 0.2 --out filter3
```

Outputs are:
```
filter0:
#ID1    ID2     NSNP    HETHET  IBS0    KINSHIP
F4      F11     1268602 0.141231        0.00994323      0.335439
F9      F2      1268974 0.124243        0.00823264      0.33746
H6      H3      1268585 0.140598        0.0103075       0.333738
H7      H4      1268526 0.133981        0.00938254      0.333626
T11     T10     1268604 0.135959        0.0100583       0.332026
T18     T10     1268547 0.134887        0.0101061       0.329433
T18     T11     1268551 0.134498        0.00997595      0.332636
T4      T3      1268537 0.136898        0.00987673      0.330729

filter 1
#ID1    ID2     NSNP    HETHET  IBS0    KINSHIP
F4      F11     562609  0.19769 0.00240842      0.438511
F9      F2      562617  0.167044        0.00188405      0.441041
H6      H3      562513  0.201496        0.00329059      0.435049
H7      H4      562532  0.186349        0.00273584      0.435394
T11     T10     562560  0.188773        0.00228953      0.439165
T18     T10     562629  0.188471        0.00220927      0.439502
T18     T11     562557  0.188399        0.00249041      0.437428
T4      T3      562615  0.188873        0.00200315      0.434651


filter 2
#ID1    ID2     NSNP    HETHET  IBS0    KINSHIP
F14     F11     350561  0.107707        0.00503193      0.228178
F4      F11     350577  0.209229        0.000601865     0.486926
F4      F14     350559  0.107528        0.00488933      0.22812
F9      F2      350562  0.179674        0.000473525     0.488625
H6      H3      350544  0.21605 0.00088434      0.484402
H7      H4      350553  0.200395        0.000696043     0.486289
T11     T10     350551  0.202741        0.000636141     0.488066
T18     T10     350557  0.202666        0.000718856     0.487308
T18     T11     350549  0.202325        0.00081301      0.486386
T4      T3      350555  0.196389        0.000758797     0.481563


filter 3
#ID1    ID2     NSNP    HETHET  IBS0    KINSHIP
F14     F11     313488  0.107028        0.00263806      0.235234
F4      F11     313500  0.213011        0.000130781     0.492497
F4      F14     313488  0.106891        0.00256788      0.235158
F9      F2      313491  0.184867        0.000178634     0.49358
H6      H3      313488  0.220771        0.00036046      0.49019
H7      H4      313496  0.206599        0.000248807     0.492149
T11     T10     313500  0.208344        0.000229665     0.492718
T18     T10     313501  0.208395        0.000223285     0.492672
T18     T11     313498  0.208174        0.000299842     0.491894
T4      T3      313491  0.199763        0.000236051     0.488114
```

So what happened? 
The kinship coefficients _increase_ as the filtration becomes more strict. That will be because these are genuine relationships and the higher filtration levels have more "true" sites consistent with the relationship and fewer error sites. OMG I think that (apart from F14) these are clones! Based off an expectation of sensible kinship values for clones, filters 2 and 3 both look good. 


## PCA
used 20 axes for the PCA, and the thinned datasets (to not crash R)

Very little observable difference with the different filtration levels. 

Filter 1:

![image](https://github.com/LydiaT12/Amanita-muscaria-population-genetics/assets/124113370/5a35e388-e4d8-4fc6-bdc1-43b3b206058b)


Most filtered (filter 4):

![image](https://github.com/LydiaT12/Amanita-muscaria-population-genetics/assets/124113370/7335071b-555e-440e-812d-505a62d303cf)



# Testing filtration levels part II

I have decided that the average kiship value over the clone pairs is a reasonable measure of the SNP error rate. 

I want to look at: 
(1) how the kiship values and dataset size change as a function of MQ cutoff in the filtering
(2) how the kiship values and dataset size change as a function of read depth bounds in the filtering
(3) how the kiship values change as a function of dataset size, for a nested set of filters.


## Datasets for investigation 1

MQ values max out at 60.

```
for mqcutoff in 20 30 40 50 55 60; do 
bcftools filter -i "MQ>=${mqcutoff}" 1.input_datasets/fullgenome_SNP.vcf.bgz -o 2.filtration_levels/MQfilter${mqcutoff}.vcf.bgz -O z;
done
```

Note: need double quotes " and not single quotes ' to ensure that the variable gets expanded. 

## Data generation for investigation 1

```
for mqcutoff in 20 30 40 50 55 60; do
plink2 --vcf ../2.filtration_levels/MQfilter${mqcutoff}.vcf.bgz --allow-extra-chr --make-pgen --out MQfilter${mqcutoff}
plink2 --pfile MQfilter${mqcutoff} --allow-extra-chr --make-king-table --king-table-filter 0.25 --out MQfilter${mqcutoff};
done
```

plink2 --vcf ../2.filtration_levels/MQfilter60.vcf.bgz --allow-extra-chr --make-pgen --out MQfilter60
plink2 --pfile MQfilter${mqcutoff} --allow-extra-chr --make-king-table --king-table-filter 0.25 --out MQfilter${mqcutoff}

## Investigation 2

Make the datasets:

```
upper = (10000 5000 4000 3500 3000 2800 2700 2600 2500 2400 2300)
lower = (0 0 0 500 1000 1200 1300 1400 1500 1600 1700)
for ii in 1..11; do 
bcftools filter -i "INFO/DP<${upper} & INFO/DP>${lower}" fullgenome_SNP.vcf.bgz -o filtration_levels/RDfilter${ii}.vcf.bgz -O z;
done
```

Now get the kinships:

```
for ii in 1..11 do
plink2 --vcf ../2.filtration_levels/RDfilter${ii}.vcf.bgz --allow-extra-chr --make-pgen --out RDfilter${ii}
plink2 --pfile RDfilter${ii} --allow-extra-chr --make-king-table --king-table-filter 0.25 --out RDfilter${ii};
done
```

## Investigation 3

Define the filtrations

```
mqcutoff = (40 40 50 50 55 60 60)
upper = (99000 5000 5000 3000 3000 3000 2500)
lower = (0 0 1000 1000 1000 1000 1500)
```

```
for ii in 1..11
do

bcftools filter -i "INFO/DP<${upper} & INFO/DP>${lower} & MQ>=${mqcutoff}" fullgenome_SNP.vcf.bgz -o filtration_levels/nestfilter${ii}.vcf.bgz -O z
plink2 --vcf ../2.filtration_levels/nestfilter${ii}.vcf.bgz --allow-extra-chr --make-pgen --outnestfilter${ii}
plink2 --pfile nestfilter${ii} --allow-extra-chr --make-king-table --king-table-filter 0.25 --out nestfilter${ii}

done
```



# Misc

Runs of homozygosity:
```
bcftools roh 2.filtration_levels/scaffolds1to133_SNPfiltered.vcf.bgz -o filtered1to133SNP.region.roh -O r
```

