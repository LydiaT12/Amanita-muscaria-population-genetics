I want to subset the clean, filtered vcf to just the MAT loci. 

```
bcftools view filtered_dataset.vcf.bgz -r Scaffold1_Amanita_muscaria:508000-515000  -Oz -o HDlocus.vcf.bgz
# Has 560 SNPs, of which 51 are multicallelic
bcftools view filtered_dataset.vcf.bgz -r Scaffold36_Amanita_muscaria:64000-100000  -Oz -o PRlocus.vcf.bgz
# Has 280 SNPs, of which 1 is multicallelic
```


Now I want to copy this vcf file to the folder I'm working in. In ```01.VariantCalling/10.MATphasing```, I call 

```
cp ../../02.Data_quality/6.All_datasets/HDlocus.vcf.bgz HD_filt_allind.vcf.bgz
cp ../../02.Data_quality/6.All_datasets/PRlocus.vcf.bgz PR_filt_allind.vcf.bgz
```

I create a vcf for each individual (this should be unnecessary, but it works)

```
forest=H     # go through each forest in turn
for num in {1..18}; do vcftools --gzvcf HD_filt_allind.vcf.bgz --indv ${forest}${num} --recode -c --recode-INFO-all | gzip -c > HDvcfs/HD.${forest}${num}.vcf.gz; done
for num in {1..18}; do vcftools --gzvcf PR_filt_allind.vcf.bgz --indv ${forest}${num} --recode -c --recode-INFO-all | gzip -c > PRvcfs/PR.${forest}${num}.vcf.gz; done

for file in HDvcfs/*; do gunzip ${file}; done
for file in PRvcfs/*; do gunzip ${file}; done
```




### WhatsHap



HD.F1.filt.vcf.gz

The bam file of reads needed to be sorted and then indexed

```
module load SAMtools

samtools sort ../3.mapped_reads/trimmed_F10_DKDN230032161-1A_HLK32DSX7_L1.unique.bam -o ../3.mapped_reads/trimmed_F10_DKDN230032161-1A_HLK32DSX7_L1.sorted.bam 
samtools index *.sorted.bam 
```



```
module load WhatsHap
forest=F
for num in {1..18}; do whatshap phase -o phased_out/HD.${forest}${num}.phased.vcf --ignore-read-groups --reference ../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta HDvcfs/HD.${forest}${num}.vcf ../3.mapped_reads/trimmed_${forest}${num}*.sorted.bam; done

for num in {1..18}; do whatshap phase -o phased_out/PR.${forest}${num}.phased.vcf --ignore-read-groups --reference ../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta PRvcfs/PR.${forest}${num}.vcf ../3.mapped_reads/trimmed_${forest}${num}*.sorted.bam; done
```

I think they all got done. 



```
forest=F
for num in {1..18}; do whatshap stats --gtf HD.${forest}${num}.phased.gtf HD.${forest}${num}.phased.vcf; done
for num in {1..18}; do whatshap stats --gtf PR.${forest}${num}.phased.gtf PR.${forest}${num}.phased.vcf; done
```     



### Reference part genome

I find that scaffold 2 starts on line 32,567 of the reference fasta file. 

```
grep -n ">Scaffold2_Amanita_muscaria" temp.fasta 
32567:>Scaffold2_Amanita_muscaria
```

I can grab the first 32567 lines into a new file:
```
grep -A 32565 ">Scaffold1_Amanita_muscaria" temp.fasta > Amanita_scaffold1.fasta
```

And index it just in case:
```
samtools faidx Amanita_scaffold1.fasta
```


I use ```grep -n Amanita Amanita_scaffold36.fasta``` to find that Scaffold 36 runs from lines 369540 to 374712

I can grab these 374712-369540 lines into a new file:
```
grep -A 5172 ">Scaffold36_Amanita_muscaria" tempy.fasta > Amanita_scaffold36.fasta
```

And index:
```
samtools faidx Amanita_scaffold36.fasta
```

### Trying to get the phased haplotypes out

In the ```phased_out``` folder I bgzip and index the phase vcf files.

```
forest=F
for num in {1..18}; 
do
bgzip HD.${forest}${num}.phased.vcf #compress variants
tabix -p vcf HD.${forest}${num}.phased.vcf.gz # index them
done
```

Now I move to a new folder and put the haplotype variants into a fasta
```
cd ../HD_fasta
module load VCFtools

for num in {1..18}; do cat ../Amanita_scaffold1.fasta | vcf-consensus -i -H 1 -s ${forest}${num} ../phased_out/HD.${forest}${num}.phased.vcf.gz  > HD.${forest}${num}.phased.1.fa; done #vcftools tool to create fasta from phased vcf H1 is first haplotype

for num in {1..18}; do cat ../Amanita_scaffold1.fasta | vcf-consensus -i -H 2 -s ${forest}${num} ../phased_out/HD.${forest}${num}.phased.vcf.gz  > HD.${forest}${num}.phased.2.fa; done # H2 is second haplotype
```

Check that it worked:
```
samtools faidx temp.fa #index fasta
samtools faidx temp.fa Scaffold1_Amanita_muscaria:10-10 #Get position 10 in fasta (1-10 would be one to ten)
```


Repeat for the PR locus:

```
forest=F
for num in {1..18}; 
do
bgzip PR.${forest}${num}.phased.vcf #compress variants
tabix -p vcf PR.${forest}${num}.phased.vcf.gz # index them
done

cd ../PR_fasta
module load VCFtools

for num in {1..18}; do cat ../Amanita_scaffold36.fasta | vcf-consensus -i -H 1 -s ${forest}${num} ../phased_out/PR.${forest}${num}.phased.vcf.gz  > PR.${forest}${num}.phased.2.fa; done #vcftools tool to create fasta from phased vcf H1 is first haplotype

for num in {1..18}; do cat ../Amanita_scaffold36.fasta | vcf-consensus -i -H 2 -s ${forest}${num} ../phased_out/PR.${forest}${num}.phased.vcf.gz  > PR.${forest}${num}.phased.2.fa; done #H2 is second haplotype
```


### Finding which regions are well phased

```
for file in phased_out/HD*.phased.gtf ;
do
base=$(basename ${file} .phased.gtf)
echo "${base}" >> HDgtfs.gtf
cat HDgtfs.gtf ${file} >> HDgtfs.gtf;
done
```

In the HD locus, most individuals have been phased to three or so blocks. The largest block is about 508227-510095bp. Many indivudals are phased from 508034 and many are phased past 511000; the 1868bp region is a very conservative cirumscription. There are only four exceptions;
F8 is only phased up to 509953
S6 is only phased from 508672
F15 is only phased from 508387
W14 has two phasing blocks in this region. 

I can subset the haplotypes to this well-phased region, and then remove W14 (manually). 

```
for file in HD_fasta/*;
do
samtools faidx ${file} 
# echo ">${file}" >> HD_phasedset.fasta # I think this bit is unnesessary. it prints stuff anyway
samtools faidx ${file} Scaffold1_Amanita_muscaria:508227-510095 >> HD_phasedset.fasta;
done
```

### Phasing with a larger dataset

The phased region could be improved, so I thought I'd try phasing with a less-filtered dataset. I copy in the one which was filtered with only MQ>=60 (has 394K variants, in contrast to the 313K variant dataset I've been working with)

```
cp ../../../02.Data_quality/2.filtration_levels/MQfilter60.vcf.bgz MQ60.vcf.bgz
```

I already have mapped reads and the reference fasta, so I just need to process the vcf before phasing. I subset to the HD region, and then split out individuals. I'll start by just testing this on W. 

```
bcftools index MQ60.vcf.bgz
bcftools view MQ60.vcf.bgz -r Scaffold1_Amanita_muscaria:508000-515000  -Oz -o HDMQ60.vcf.bgz
# Has 710 SNPs, of which 59 are multiallelic SNPs and 108 are "multiallelic"
#bcftools view filtered_dataset.vcf.bgz -r Scaffold36_Amanita_muscaria:64000-100000  -Oz -o PRlocus.vcf.bgz
## Has 280 SNPs, of which 1 is multicallelic
```

```
forest=W     # go through each forest in turn
for num in {8..26}; do vcftools --gzvcf HDMQ60.vcf.bgz --indv ${forest}${num} --recode -c --recode-INFO-all | gzip -c > HDvcfs/HDMQ60.${forest}${num}.vcf.gz; done

for file in HDvcfs/*; do gunzip ${file}; done
```

```
module load WhatsHap
forest=W
for num in {8..26}; do whatshap phase -o phased_out/HDMQ60.${forest}${num}.phased.vcf --ignore-read-groups --reference ../../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta HDvcfs/HDMQ60.${forest}${num}.vcf ../../3.mapped_reads/trimmed_${forest}${num}*.sorted.bam; done

cd phased_out
for num in {8..26}; do whatshap stats --gtf HDMQ60.${forest}${num}.phased.gtf HDMQ60.${forest}${num}.phased.vcf; done
```

Then I repeated for MQ55 for comparison. 

## Phase the 1st degree relatives

```
cp ../../02.Data_quality/6.All_datasets/filtered_dataset.vcf.bgz tempall.vcf.bgz
```

```
vcftools --gzvcf tempall.vcf.bgz --indv F4 --recode -c --recode-INFO-all > fullgenomerelatives/fullgenome.F4.vcf
vcftools --gzvcf tempall.vcf.bgz --indv F14 --recode -c --recode-INFO-all > fullgenomerelatives/fullgenome.F14.vcf
```

Now do the phasing:
```
module load WhatsHap
whatshap phase -o fullgenome.F4.phased.vcf --ignore-read-groups --reference ../../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta fullgenome.F4.vcf ../../3.mapped_reads/trimmed_F4*.sorted.bam
```



