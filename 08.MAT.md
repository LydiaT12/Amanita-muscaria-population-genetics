I want to subset the clean, filtered vcf to just the MAT loci. 

```
bcftools view filtered_dataset.vcf.bgz -r Scaffold1_Amanita_muscaria:508000-515000  -Oz -o HDlocus.vcf.bgz
# Has 560 SNPs, of which 51 are multicallelic
bcftools view filtered_dataset.vcf.bgz -r Scaffold36_Amanita_muscaria:64000-100000  -Oz -o PRlocus.vcf.bgz
# Has 280 SNPs, of which 1 is multicallelic
```


Now I want to copy this vcf file to the folder I'm working in. In ```01.VariantCalling/10.MATphasing```, I call 

```
cp ../../02.Data_quality/6.All_datasets/HDlocus.vcf.bgz HD_filt_allind.vcf.bgz
cp ../../02.Data_quality/6.All_datasets/PRlocus.vcf.bgz PR_filt_allind.vcf.bgz
```

I create a vcf for each individual (this should be unnecessary, but it works)

```
forest=H     # go through each forest in turn
for num in {1..18}; do vcftools --gzvcf HD_filt_allind.vcf.bgz --indv ${forest}${num} --recode -c --recode-INFO-all | gzip -c > HDvcfs/HD.${forest}${num}.vcf.gz; done
for num in {1..18}; do vcftools --gzvcf PR_filt_allind.vcf.bgz --indv ${forest}${num} --recode -c --recode-INFO-all | gzip -c > PRvcfs/PR.${forest}${num}.vcf.gz; done

for file in HDvcfs/*; do gunzip ${file}; done
for file in PRvcfs/*; do gunzip ${file}; done
```




### WhatsHap



HD.F1.filt.vcf.gz

The bam file of reads needed to be sorted and then indexed

```
module load SAMtools

samtools sort ../3.mapped_reads/trimmed_F10_DKDN230032161-1A_HLK32DSX7_L1.unique.bam -o ../3.mapped_reads/trimmed_F10_DKDN230032161-1A_HLK32DSX7_L1.sorted.bam 
samtools index *.sorted.bam 
```



```
module load WhatsHap
forest=F
for num in {1..18}; do whatshap phase -o phased_out/HD.${forest}${num}.phased.vcf --ignore-read-groups --reference ../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta HDvcfs/HD.${forest}${num}.vcf ../3.mapped_reads/trimmed_${forest}${num}*.sorted.bam; done

for num in {1..18}; do whatshap phase -o phased_out/PR.${forest}${num}.phased.vcf --ignore-read-groups --reference ../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta PRvcfs/PR.${forest}${num}.vcf ../3.mapped_reads/trimmed_${forest}${num}*.sorted.bam; done
```

I think they all got done. 



```
forest=F
for num in {1..18}; do whatshap stats --gtf HD.${forest}${num}.phased.gtf HD.${forest}${num}.phased.vcf; done
for num in {1..18}; do whatshap stats --gtf PR.${forest}${num}.phased.gtf PR.${forest}${num}.phased.vcf; done
```     



### Reference part genome

I find that scaffold 2 starts on line 32,567 of the reference fasta file. 

```
grep -n ">Scaffold2_Amanita_muscaria" temp.fasta 
32567:>Scaffold2_Amanita_muscaria
```

I can grab the first 32567 lines into a new file:
```
grep -A 32565 ">Scaffold1_Amanita_muscaria" temp.fasta > Amanita_scaffold1.fasta
```

And index it just in case:
```
samtools faidx Amanita_scaffold1.fasta
```


I use ```grep -n Amanita Amanita_scaffold36.fasta``` to find that Scaffold 36 runs from lines 369540 to 374712

I can grab these 374712-369540 lines into a new file:
```
grep -A 5172 ">Scaffold36_Amanita_muscaria" tempy.fasta > Amanita_scaffold36.fasta
```

And index:
```
samtools faidx Amanita_scaffold36.fasta
```

### Trying to get the phased haplotypes out

In the ```phased_out``` folder I bgzip and index the phase vcf files.

```
forest=F
for num in {1..18}; 
do
bgzip HD.${forest}${num}.phased.vcf #compress variants
tabix -p vcf HD.${forest}${num}.phased.vcf.gz # index them
done
```

Now I move to a new folder and put the haplotype variants into a fasta
```
cd ../HD_fasta
module load VCFtools

for num in {1..18}; do cat ../Amanita_scaffold1.fasta | vcf-consensus -i -H 1 -s ${forest}${num} ../phased_out/HD.${forest}${num}.phased.vcf.gz  > HD.${forest}${num}.phased.1.fa; done #vcftools tool to create fasta from phased vcf H1 is first haplotype

for num in {1..18}; do cat ../Amanita_scaffold1.fasta | vcf-consensus -i -H 2 -s ${forest}${num} ../phased_out/HD.${forest}${num}.phased.vcf.gz  > HD.${forest}${num}.phased.2.fa; done # H2 is second haplotype
```

Check that it worked:
```
samtools faidx temp.fa #index fasta
samtools faidx temp.fa Scaffold1_Amanita_muscaria:10-10 #Get position 10 in fasta (1-10 would be one to ten)
```


Repeat for the PR locus:

```
forest=F
for num in {1..18}; 
do
bgzip PR.${forest}${num}.phased.vcf #compress variants
tabix -p vcf PR.${forest}${num}.phased.vcf.gz # index them
done

cd ../PR_fasta
module load VCFtools

for num in {1..18}; do cat ../Amanita_scaffold36.fasta | vcf-consensus -i -H 1 -s ${forest}${num} ../phased_out/PR.${forest}${num}.phased.vcf.gz  > PR.${forest}${num}.phased.2.fa; done #vcftools tool to create fasta from phased vcf H1 is first haplotype

for num in {1..18}; do cat ../Amanita_scaffold36.fasta | vcf-consensus -i -H 2 -s ${forest}${num} ../phased_out/PR.${forest}${num}.phased.vcf.gz  > PR.${forest}${num}.phased.2.fa; done #H2 is second haplotype
```


### Finding which regions are well phased

```
for file in phased_out/HD*.phased.gtf ;
do
base=$(basename ${file} .phased.gtf)
echo "${base}" >> HDgtfs.gtf
cat HDgtfs.gtf ${file} >> HDgtfs.gtf;
done
```

In the HD locus, most individuals have been phased to three or so blocks. The largest block is about 508227-510095bp. Many indivudals are phased from 508034 and many are phased past 511000; the 1868bp region is a very conservative cirumscription. There are only four exceptions;
F8 is only phased up to 509953
S6 is only phased from 508672
F15 is only phased from 508387
W14 has two phasing blocks in this region. 

I can subset the haplotypes to this well-phased region, and then remove W14 (manually). 

```
for file in HD_fasta/*;
do
samtools faidx ${file} 
# echo ">${file}" >> HD_phasedset.fasta # I think this bit is unnesessary. it prints stuff anyway
samtools faidx ${file} Scaffold1_Amanita_muscaria:508227-510095 >> HD_phasedset.fasta;
done
```

### Phasing with a larger dataset

The phased region could be improved, so I thought I'd try phasing with a less-filtered dataset. I copy in the one which was filtered with only MQ>=50 (has 711K variants, in contrast to the 313K variant dataset I've been working with)

```
cp ../../../02.Data_quality/2.filtration_levels/MQfilter50.vcf.bgz MQ50.vcf.bgz
```

I already have mapped reads and the reference fasta, so I just need to process the vcf before phasing. I subset to the HD region, and then split out individuals.

```
bcftools index MQ50.vcf.bgz
bcftools view MQ50.vcf.bgz -r Scaffold1_Amanita_muscaria:508000-515000  -Oz -o HDMQ50.vcf.bgz
# Has 1885 SNPs, of which 559 are multiallelic and 240 of those are multiallelic SNPs
bcftools view MQ50.vcf.bgz -r Scaffold36_Amanita_muscaria:64000-100000  -Oz -o PRMQ50.vcf.bgz
## Has 398 SNPs, of which 14 are multiallelic and 5 are multiallelic SNPs
```

```
forest=W     # go through each forest in turn
for num in {8..28}; do vcftools --gzvcf HDMQ50.vcf.bgz --indv ${forest}${num} --recode -c --recode-INFO-all | gzip -c > HDvcfs/HDMQ50.${forest}${num}.vcf.gz; done
for num in {8..28}; do vcftools --gzvcf PRMQ50.vcf.bgz --indv ${forest}${num} --recode -c --recode-INFO-all | gzip -c > PRvcfs/PRMQ50.${forest}${num}.vcf.gz; done

for file in HDvcfs/*; do gunzip ${file}; done
for file in PRvcfs/*; do gunzip ${file}; done
```

Now I do the phasing, and create a gtf file which recoreds where the phasing blocks are. 

```
module load WhatsHap
forest=W
for num in {8..28}; do whatshap phase -o phased_out/HDMQ50.${forest}${num}.phased.vcf --ignore-read-groups --reference ../../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta HDvcfs/HDMQ50.${forest}${num}.vcf ../../3.mapped_reads/trimmed_${forest}${num}*.sorted.bam; done
for num in {8..28}; do whatshap phase -o phased_out/PRMQ50.${forest}${num}.phased.vcf --ignore-read-groups --reference ../../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta PRvcfs/PRMQ50.${forest}${num}.vcf ../../3.mapped_reads/trimmed_${forest}${num}*.sorted.bam; done   ## this step in PR locus done for W only 

for file in phased_out/PR*;
do
base=$(basename ${file} .vcf)
whatshap stats --gtf phased_out/${base}.gtf ${file};
done
```


Now I want to look at the regions where these are phased. I concatenate all the gtf files

```
for file in phased_out/PR*.phased.gtf ;
do
base=$(basename ${file} .phased.gtf)
echo "${base}" >> PRgtfs.gtf
cat PRgtfs.gtf ${file} >> PRgtfs.gtf;
done
```





#### Trying to get the phased haplotypes out

In the ```phased_out``` folder I bgzip and index the phased vcf files.

```
for file in phased_out/HD*.phased.vcf ;
do
bgzip ${file}
tabix -p vcf ${file}.gz;
done
```


Now I move to a new folder and put the haplotype variants into a fasta
```
cd ../HDfastas
module load VCFtools
for file in ../phased_out/HD*.phased.vcf.gz ;
do
base=$(basename ${file} .phased.vcf.gz)
code=${base#*.}
cat ../../Amanita_scaffold1.fasta | vcf-consensus -i -H 1 -s ${code} ../phased_out/${base}.phased.vcf.gz  > ${base}.phased.1.fa #vcftools tool to create fasta from phased vcf H1 is first haplotype
cat ../../Amanita_scaffold1.fasta | vcf-consensus -i -H 2 -s ${code} ../phased_out/${base}.phased.vcf.gz  > ${base}.phased.2.fa;
done

```

Check that it worked:
```
samtools faidx HDMQ50.F10.phased.1.fa #index fasta
samtools faidx HDMQ50.F10.phased.2.fa
samtools faidx HDMQ50.F10.phased.1.fa Scaffold1_Amanita_muscaria:10-10 #Get position 10 in fasta (1-10 would be one to ten)
```

Now subset to well-phased regions
```
for file in HDfastas/*.fa;
do
# samtools faidx ${file}
base=$(basename ${file} .fa)
code=${base#*.}
echo ">${file}" >> HD_phasedset.a.fasta
samtools faidx ${file} Scaffold1_Amanita_muscaria:508200-510000 >> HD_phasedset.a.fasta;
done
```

```
for file in HDfastas/*.fa;
do
echo ">${file}" >> HD_phasedset.b.fasta
samtools faidx ${file} Scaffold1_Amanita_muscaria:510500-511300 >> HD_phasedset.b.fasta;
done

for file in HDfastas/*.fa;
do
# echo ">${file}" >> HD_phasedset.c.fasta
samtools faidx ${file} Scaffold1_Amanita_muscaria:511500-512100 >> HD_phasedset.c.2.fasta;
done

for file in HDfastas/*.fa;
do
echo ">${file}" >> HD_phasedset.d.fasta
samtools faidx ${file} Scaffold1_Amanita_muscaria:512800-513900 >> HD_phasedset.d.fasta;
done
```

Then I repeated (c) and (d) without echoing ">${file}" and added a .2


Get a list of sample allele names

```
for file in HDfastas/*.fa.fai ;
do
base=$(basename ${file} .fa.fai)
echo "${base}" >>names.tsv;
done
```
Then run change_names.sl to append the correct name to each entry of the fasta


I looked manually at the phased regions to decide which well-phased fragments can be studied (above). For each phased region, some individuals were not phased over the region. These were identified and deleted manually (in HD_phasedset.*.named.fasta). Some individuals were phased over part of the region. Region (c) phased particularly poorly, and I kept samples which were phased over at least half of the region. This was checked manually; unphased bits of heterozygous genome were replaced with N in these samples (manually). 
These samples were manually deleted from (a): S6, S12, W14
(b): W13, W17
(c): F5, F9,F14,F15,F17, H1,H14, S8,S15,S18, T2,T5,T7,T8,T12,T14, W3,W12,W16,W17,W18,w28
(d) : F13, F17, H17, S3, S8, T12, T13

<!--
These parts of samples were replaced with N in region (c):
W25  >512000  (100bp)
W26  <511743  (97bp)
W25  <511677 and >512071  (22 and 29bp)
W20  >512089  (11bp)
W13  >512083  (17bp)
T10  >512042  (58bp)
S9  <511755  (105bp)
S7  <511743 and >512075  (93 and 25bp)
S4  >511932  (168bp)
S3  >512000  (100bp)
S10  >512000  (100bp)
F8  <511788  (138bp)
-->





# Doing stuff with the phased mating types

First I move my nice (?) HD datasets to a new directory called MAT

```
cp ../../01.VariantCalling/10.MATphasing/MQ50phase/HD_phasedset.a.temp.fasta HDa.fasta
cp ../../01.VariantCalling/10.MATphasing/MQ50phase/HD_phasedset.b.named.fasta HDb.fasta
cp ../../01.VariantCalling/10.MATphasing/MQ50phase/HD_phasedset.c.named.fasta HDc.fasta
cp ../../01.VariantCalling/10.MATphasing/MQ50phase/HD_phasedset.d.named.fasta HDd.fasta
```

I want a phylogeny. mafft alignment doesn't like "." Apparently whatshap uses * for deletions and "." for missing data

```
mafft HDa.fasta > aligned_HDa.fa #align sequences
seqmagick convert aligned_HDa.fa HDa.phy #convert fasta to phylip
```

seqmagick gives an error because it truncates the sequence names and ends up with repeated names. 
> Repeated name 'F10.phased' (originally 'F10.phased.2.Scaffold1_Amanita_muscaria:508200-510000_HDMQ50.F10.phased.2'), possibly due to truncation
iqtree2 -s ITS.phy -st DNA -m GTR+G -bb 1000  -pre ITS






## Phase the 1st degree relatives

```
cp ../../02.Data_quality/6.All_datasets/filtered_dataset.vcf.bgz tempall.vcf.bgz
```

```
vcftools --gzvcf tempall.vcf.bgz --indv F4 --recode -c --recode-INFO-all > fullgenomerelatives/fullgenome.F4.vcf
vcftools --gzvcf tempall.vcf.bgz --indv F14 --recode -c --recode-INFO-all > fullgenomerelatives/fullgenome.F14.vcf
```

Now do the phasing:
```
module load WhatsHap
whatshap phase -o fullgenome.F4.phased.vcf --ignore-read-groups --reference ../../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta fullgenome.F4.vcf ../../3.mapped_reads/trimmed_F4*.sorted.bam
```

Get the stats:

```
whatshap stats --gtf fullgenome.F4.phased.gtf fullgenome.F4.phased.vcf
```



## Genome stats near the mating type loci

For each mating type locis, I have a specific and a broad guess as to where it is. I want to do vcf stats on each of these (4) domains. 

Broad:
PR: Scaffold 36, 64,000 to 100,000bp exists: PRlocus.vcf.bgz
HD: Scaffold 1, 508,000 to 515,000bp exists: HDlocus.vcf.bgz
Specific:
HD: 510000 - 513500, S1
PR: 78500 - 103500, S36

Make the two specific datasets
```
bcftools view ../../02.Data_quality/6.All_datasets/filtered_dataset.vcf.bgz -r Scaffold1_Amanita_muscaria:510000-513500  -Oz -o HDspecific.vcf.bgz
# Has 240 SNPs, of which 42 are multicallelic
bcftools view ../../02.Data_quality/6.All_datasets/filtered_dataset.vcf.bgz -r Scaffold36_Amanita_muscaria:78500-103500  -Oz -o PRspecific.vcf.bgz
# Has 178 SNPs, of which 1 is multicallelic
```


### still to do
maybe make another "broad" vcf file, because the overlaps in the PR files are bad?
```
bcftools view ../../02.Data_quality/6.All_datasets/filtered_dataset.vcf.bgz -r Scaffold36_Amanita_muscaria:60000-105000  -Oz -o PRbroad.vcf.bgz
```
Now, what do I want to do?
1) run a mini pixy
2) heterozygosity
3) dN/dS PAML https://pubmed.ncbi.nlm.nih.gov/37096789/

vcftools is good for heterozygosity, SFS and related measures
```
vcftools --gzvcf ../../02.Data_quality/6.All_datasets/HDlocus.noclone.vcf.gz --counts2 
```
https://vcftools.github.io/man_latest.html



1 pixy
```
module load Miniconda3
source activate pixyenvt

#gunzip -c 8.Invariants/mito.allsites2.noclone.vcf.gz | bgzip  > 8.Invariants/mito.allsites2.noclone.vcf.bgz
bcftools view ../../02.Data_quality/8.Invariants/invariants_1to28.noclone.vcf.bgz -r Scaffold1_Amanita_muscaria  -Oz -o scaffold1_noclone.vcf.bgz
tabix scaffold1_noclone.vcf.bgz # index the datasets
pixy --stats pi dxy fst  --vcf scaffold1_noclone.vcf.bgz --populations ../popfile_noclone.id  --window_size 200 --output_prefix scaffold1
```





