I want to subset the clean, filtered vcf to just the MAT loci. 

```
bcftools view filtered_dataset.vcf.bgz -r Scaffold1_Amanita_muscaria:508000-515000  -Oz -o HDlocus.vcf.bgz
# Has 560 SNPs, of which 51 are multicallelic
bcftools view filtered_dataset.vcf.bgz -r Scaffold36_Amanita_muscaria:64000-100000  -Oz -o PRlocus.vcf.bgz
# Has 280 SNPs, of which 1 is multicallelic
```


Remove the clones:
```
vcftools --gzvcf HDlocus.vcf.bgz --remove-indv F11 --remove-indv F2 --remove-indv H3 --remove-indv H4 --remove-indv T10 --remove-indv T18 --remove-indv T4 --recode -c --recode-INFO-all | gzip -c > HDlocus.noclone.vcf.gz
vcftools --gzvcf PRlocus.vcf.bgz --remove-indv F11 --remove-indv F2 --remove-indv H3 --remove-indv H4 --remove-indv T10 --remove-indv T18 --remove-indv T4 --recode -c --recode-INFO-all | gzip -c > PRlocus.noclone.vcf.gz
```



```
vcftools --gzvcf HD_filt_allind.vcf.bgz --indv F1 --recode -c --recode-INFO-all | gzip -c > HD.F1.filt.vcf.gz
```


## Phasing
```
cp 5.hapcalling/trimmed_F1_DKDN230032152-1A_HLK32DSX7_L1.g.vcf.gz 10.MATphasing/F1.g.vcf.gz
cp ../5.hapcalling/trimmed_F1_DKDN230032152-1A_HLK32DSX7_L1.g.vcf.gz.tbi F1.g.vcf.gz.tbi
bcftools index F1.g.vcf.gz 
bcftools view F1.g.vcf.gz -r Scaffold1_Amanita_muscaria:508000-515000  -Oz -o HDlocus_F1.vcf.bgz
bcftools view F1.g.vcf.gz -r Scaffold1_Amanita_muscaria:508000-515000  -Ov -o HDlocus_F1.vcf
```

Poking around in this file, I see that some (but not all) of the heterozygous loci have phasing information. The attribute PGT records physical phasing with a character like 0|1 to denote which read is the alternate. PID is a phasing ID, looking like 508297_A_G. PS is a phasing set, typically the base of the first phased allele in the set. I think that I should filter by having a PS attribute and then see how easy it is to cluster these groups. 

```
# gatk VariantFiltration  -R ../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta  -V HDlocus_F1.vcf.bgz  --filter-name "PS" --filter-expression "vc.hasAttribute("PS")" -O HD_F1.filtered.g.vcf.gz
gatk IndexFeatureFile -I HDlocus_F1.vcf.bgz
gatk VariantFiltration  -R ../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta  -V HDlocus_F1.vcf.bgz  --genotype-filter-name "PS" --genotype-filter-expression "PS>500000" -O HD_F1.filtered.g.vcf.gz
gatk VariantFiltration  -R ../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta  -V HDlocus_F1.vcf.bgz  --genotype-filter-name "PS" --genotype-filter-expression "vc.hasAttribute('PS') && PS>500000" -O HD_F1.filtered.g.vcf.gz
```

PS is in the FORMAT field, therefore needs to be filtered with genotype-filter instead of the -filter, which looks at stuff in the INFO field. 
This does nothing. Everything passes, whether I can see a PS attribute or not. Maybe I should filter by being heterozogous instead???





### WhatsHap

<!--
The vcf of everything is in ```../../02.Data_quality/6.All_datasets/filtered_dataset.vcf.bgz```. 

I wanna subset to just one individual

```
vcftools --gzvcf PRlocus.vcf.bgz --indv F1 --recode -c --recode-INFO-all | gzip -c > PRlocus.noclone.vcf.gz
```
-->

I have ```HDlocus_F1.vcf``` in this folder.  

The bam file of reads needed to be sorted and then indexed


```
module load WhatsHap 
whatshap phase -o HD.F1.phased.vcf --reference ../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta HDlocus_F1.vcf reads.F1.unique.sorted.bam       #This basically got 0 informative reads/variants
whatshap phase -o HD.F1.filt.phased.vcf --ignore-read-groups --reference ../1.ref_genome/Amanita_muscaria_genome_2024_masked.fasta HD.F1.filt.vcf.gz reads.F1.unique.sorted.bam    #***this worked!
```

Verbose comments on the phasing of the filtered file:

```
Found 101 usable heterozygous variants (0 skipped due to missing genotypes)
Found 524 reads covering 101 variants
Kept 465 reads that cover at least two variants each
Selected 208 most phase-informative reads covering 101 variants
Best-case phasing would result in 5 non-singleton phased blocks (0 singletons). 
Phasing 1 sample by solving the MEC problem ...
Largest block contains 68 variants (67.3% of accessible variants) between position 508037 and 510100
```

```
whatshap stats --gtf HD.F1.filt.phased.gtf HD.F1.filt.phased.vcf
```

Output: 

```
     Variants in VCF:      485
        Heterozygous:      101 (     101 SNVs)
              Phased:      101 (     101 SNVs)
            Unphased:        0 (not considered below)
          Singletons:        0 (not considered below)
              Blocks:        5

Block sizes (no. of variants)
   Median block size:        6.00 variants
  Average block size:       20.20 variants
       Largest block:       68    variants
      Smallest block:        4    variants

Block lengths (basepairs)
      Sum of lengths:     3627    bp
 Median block length:      261.00 bp
Average block length:      725.40 bp
       Longest block:     2063    bp
      Shortest block:       42    bp
          Block NG50:        0    bp
```
